apply from: "$rootProject.projectDir/$configModuleName/subModuleHeader.gradle"

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(":commonbiz")
}

globleConfigure()